package dungeonDragon;

import java.util.*;

public class PlayGame {
	
	/* public String playGame() {
		if(warriorList.size() == 0 && wizzardList.size() == 0) {
			System.out.println("You have to create a character to start the game...");
		}
		else {
			System.out.println("You have created your character, now roll the dice to advance by choosing 4:");
		}
	} */
	
	public static void main(String[] args) {
		//
	}
	
	/*public PlayGame() {
		this("Undefined game");
	}
	
	public PlayGame(Dice dice) {
		
	}

	public void charactersReady() {
		if(warriorList.size() == 0 && wizzardList.size() == 0) {
			System.out.println("You have to create a character to start the game...");
		}
		else {
			
			System.out.println("You have created your character, now roll the dice to advance by choosing 4:");

			public int playGame(Scanner clavier) {
				System.out.println("Press enter to roll the dice and move to the corresponding square on the board : ");
				clavier.nextLine();
				int launchDice = Dice.dice(1);
		        int lifeLvl = launchDice;
			}
			
			playGame(clavier);
		}
	} */
	
	
}
